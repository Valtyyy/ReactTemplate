import { Layout } from "antd"


export const TableLayout_1 = ({children}) => {

    return(
        <Layout style={{
            padding: '10px'
        }}>
            {children}
        </Layout>
    )
}